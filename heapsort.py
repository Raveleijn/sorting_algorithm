import math

def Heapify(list):
  heap = []
  for i in list:
    inserting = True
    treePos = len(heap)
    heap.append(i)
    while inserting:
      parentPos = math.floor(treePos-1)
      if 
    